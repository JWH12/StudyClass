package 스트림;

public class 이론 {
	
	
	/*
	  
	  buffer라는 메소드를 사용하면 입출력 시간이 기하급수적으로 줄어든다.
	  
	  out.flush(); // flush()라는 메소드를 통하여 남은 버퍼를 모두 출력시킴
	 
	 ====================== File 클래스 ============================
	 파일의 경로명을 다루는 클래스
	 
	 파일 관리 기능
 	 파일 이름 변경, 삭제, 디렉터리 생성, 크기 등 파일 관리
	 File 객체는 파일 읽고 쓰기 기능 없음
	 
	 
	 
	 String filename = f.getName(); // "system.ini"              // 지정된 경로에서 파일 이름만 가져옴
	 String path = f.getPath(); // "c:\\windows\\system.ini"		// 모든 경로를 가져옴
	 String parent = f.getParent(); // "c:\\windows"			    // 
	 
	 
	 
	 파일인지 디렉터리인지 구분 하기 위한 코드

	 if(f.isFile()) // 파일인 경우
	 System.out.println(f.getPath() + "는 파일입니다.");
	 else if(f.isDirectory()) // 디렉터리인 경우
	 System.out.println(f.getPath() + "는 디렉터리입니다."
	 
	 
	 
	 */

}
