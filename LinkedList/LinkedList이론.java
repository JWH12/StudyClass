package LinkedList;

public class LinkedList이론 {
	/*
	 
	 1-2 LinkedList
	 => 객체를 만들고 만들어진 객체와 다른 객체를 연결한다.
	 => ArrayList와는 다르게 배열을 사용하지 않는다.
	 => 노드라는 객체를 만들고 노드끼리 연결된다. 
	 => Link field는 변수이며 다음 리스트가 누구인지에 대한 주소를 저장하고있다.
	 => 링크드리스트에서 어떤게 첫번째 노드인지를 알려주는 변수를 내부적으로 가지고 있어야한다.
	 => 어떤게 제일 마지막에 쓰여지는 노드인지를 기입해야한다.(기입을 안하는 경우도 있다.)
	 => 몇개의 엘리먼트가 있는지 알려주는 size()변수가 존재한다, 

	  
	  
	 */
	

}
