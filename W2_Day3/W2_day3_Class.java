package W2_Day3;

import java.util. *; // java.util에 있는 다른 패키지들을 모두 쓰고 싶으면 *을 사용한다
					 // IT에서는 *이 모두를 뜻한다.

import java.lang. *; // 자바 language패키지로서 스트링, 수학 함수, 입출력 등 자바 프로그밍에 필요한 기본적인 클래스와 인터페이스


public class W2_day3_Class {

	
	/*
	 
	 ================ Object ======================
	 1. 모든 클래스가 상속받는 클래스로서 모든 클래스의 슈퍼클래스이다.
	 
	 
	 ================ equals() ====================
	 
	 
	 ================ Wrapper =====================
	 
	 ================ String =====================
	 
	 String과 같은 문자열을 비교할때는 equals()를 사용하여 비교한다.
	 
	 
	 
	 ================ concat =====================
	  
	  
	 문자열을 이어붙이는 성능을 한다.
	 
	 
	  ================ Math (클래스)=====================
	 
	 산술 연산 메소드 제공, java.lang.Math
	 
	 max, min을 가장 많이 사용한다.
	 
	 
	 ================ Calendar (클래스)=====================
	 
	 Calendar now = Calendar.getInstance();
	 
	 시간과 날짜 정보 저장 관리
	 
	 int year = now.get(Calendar.YEAR); 			// now에 저장된 년도를 가져오기 위해서는 get메소드를 사용한다.
	 
	 int month = now.get(Calendar.MONTH) + 1;		// now에 저장된 달
	 
	 
	 Calendar firstDate = Calendar.getInstance();
	 
	 firstDate.clear(); // 현재 날짜와 시간 정보를 모두 지운다.
	 
	 firstDate.set(2016, 11, 25); // 2016년 12월 25일. 12월은 11로 설정
	 
	 
	 */
	
	
	
	
	
	
}
