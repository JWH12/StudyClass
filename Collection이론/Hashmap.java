package Collection이론;

public class Hashmap {
	
	
	/*
	 => key 값은 중복이 불가능한다.
	 => value는 중복이 가능하다.
		  
	  ex) HashMap<String, Integer> a = new HashMap<String(=key 타입설정) , Integer(=value타입 설정)>)();
	  	   
	  	  a.put("one"(= key 값),1(= value 값));			// put은 map인터페이스에서만 존재하는 API이며 두개의 인자를 가진다.
	  	  a.put("two",2);
	  	  a.put("three",3);
	 	  a.put("four",4);
	 	  
	 	  System.out.println(a.get("one"));				// key 값인 "one"에 입력된 value값인 1을 리턴한다. 
	      System.out.println(a.get("one"));
	      System.out.println(a.get("one"));
	      
	        
	      iteratorUsingForEach(a);
	      iteratorUsingIterator(a);		
	 */
	

}
